version: "2"

run:
  timeout: 5m
  tests: false

linters:
  enable:
    - govet
    - ineffassign
  disable:
    - errcheck
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
