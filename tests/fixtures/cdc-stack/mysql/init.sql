CREATE TABLE IF NOT EXISTS cdc_events (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
GRANT REPLICATION SLAVE, REPLICATION CLIENT ON *.* TO 'effectus'@'%';
