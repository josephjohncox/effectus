flow "MIXED_PROCESS" priority 3 {
    when {
        order.type == "precision"
        customer.priority >= 2
    }
    steps {
        analyze_requirements order_id:order.id specs:order.specs -> req
        create_timeline requirements:$req milestone:"design"     -> timeline
        notify_engineering timeline:$timeline reason:"New design requirement"
    }
} 