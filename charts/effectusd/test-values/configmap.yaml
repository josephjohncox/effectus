image:
  tag: "0.2.1"
postgres:
  existingSecret: effectusd-postgres
bundle:
  cacheDir: /data/bundles
  signatureVerifier: /usr/local/bin/effectus-verify-oci
api:
  existingSecret: effectusd-api
config:
  enabled: true
  contents: |
    bundle:
      oci: ghcr.io/example/bundles/demo@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      cache_dir: /ignored/read-only
    http:
      addr: ":18080"
    metrics:
      addr: ":19090"
rolloutNonce: "configmap-contract"
